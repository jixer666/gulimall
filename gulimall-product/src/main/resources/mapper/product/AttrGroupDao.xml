<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ljx.gulimall.product.dao.AttrGroupDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.ljx.gulimall.product.model.entity.AttrGroupEntity" id="attrGroupMap">
        <result property="attrGroupId" column="attr_group_id"/>
        <result property="attrGroupName" column="attr_group_name"/>
        <result property="sort" column="sort"/>
        <result property="descript" column="descript"/>
        <result property="icon" column="icon"/>
        <result property="catelogId" column="catelog_id"/>
    </resultMap>

    <resultMap type="com.ljx.gulimall.product.model.vo.SpuItemAttrGroupVO" id="spuAttrGroupMap">
        <result property="groupName" column="attr_group_name"/>
        <collection property="attrValues" ofType="com.ljx.gulimall.product.model.vo.SpuBaseAttrVO">
            <result property="attrName" column="attr_name"></result>
            <result property="attrValue" column="attr_value"></result>
        </collection>
    </resultMap>

    <select id="selectAttrGroupBySpuIdAndCatelogId" resultMap="spuAttrGroupMap">
        select ag.attr_group_name, ag.attr_group_id, pa.attr_id, pa.attr_name, pav.attr_value from pms_attr_group ag
        left join pms_attr_attrgroup_relation aar on ag.attr_group_id = aar.attr_group_id
        left join pms_attr pa on aar.attr_id = pa.attr_id
        left join pms_product_attr_value pav on pav.attr_id = pa.attr_id
        where pav.spu_id = #{spuId} and ag.catelog_id = #{categoryId}
    </select>


</mapper>